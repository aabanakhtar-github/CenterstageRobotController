package org.firstinspires.ftc.teamcode.tests;

import com.qualcomm.robotcore.eventloop.opmode.LinearOpMode;
import com.qualcomm.robotcore.eventloop.opmode.TeleOp;

import org.firstinspires.ftc.robotcore.external.hardware.camera.WebcamName;
import org.firstinspires.ftc.teamcode.components.BluePropDetector;
import org.firstinspires.ftc.vision.VisionPortal;
/*
@TeleOp(name = "Camera Test")
*/

public class CameraTest {
/*
    BluePropDetector m_Pipeline;

    VisionPortal m_VisionPortal;

    @Override
    public void runOpMode() throws InterruptedException {
        m_Pipeline = new BluePropDetector();
        m_VisionPortal = VisionPortal.easyCreateWithDefaults(hardwareMap.get(WebcamName.class, "Webcam 1"), m_Pipeline);

        while (opModeInInit() && !isStopRequested()) {

            telemetry.addData("Detection location:", m_Pipeline.GetDetectionPosition().toString());
            telemetry.addData("hi", "bo");
            telemetry.update();
        }

        waitForStart();
    }

*/
}
